version: '3.8'

services:
  backend:
    build: .
    container_name: warteg-backend
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - warteg-network

networks:
  warteg-network:
    driver: bridge
